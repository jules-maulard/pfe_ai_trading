name: compute_indicators

symbols:
  source: storage   # compute for all symbols present in the database

dates:
  mode: full   # load all available OHLCV history for computation
  # mode: fixed
  # start_date: "2026-01-01"
  # end_date: "2026-04-01"

storage: snowflake

fetch:
  ohlcv: false
  dividends: false
  asset_info: false

indicators:
  enabled: true
  compute:
    # - rsi
    - macd
    # - pivot
  # symbols: null  # null = same symbols as resolved above
