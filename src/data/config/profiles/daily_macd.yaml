name: daily_macd

symbols:
  source: storage

dates:
  mode: incremental

storage: snowflake

fetch:
  ohlcv: false
  dividends: false
  asset_info: false

indicators:
  enabled: true
  compute:
    - macd
